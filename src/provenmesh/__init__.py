"""ProvenMesh — Evidence-first Intelligence Graph Pipeline.

An async-first, idempotent, distributed ingestion system for
GraphOne/FrontierAtlas where every discovered item is durably stored,
every extracted field is traceable to source evidence, every uncertain
entity is reviewable, every relationship is explicitly modeled, and
every stage can be retried or horizontally scaled without corrupting
the graph.
"""

__version__ = "1.0.0"
__author__ = "Suraj Sharma"
