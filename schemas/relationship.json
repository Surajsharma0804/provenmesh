{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://provenmesh.dev/schemas/relationship.json",
  "title": "Relationship Edge",
  "description": "Explicit graph edge between entities (PDF §8.2, Appendix D)",
  "type": "object",
  "required": ["source_id", "target_id", "relation_type", "confidence", "source_url", "collected_at"],
  "properties": {
    "source_id": {
      "type": "string",
      "description": "Canonical ID of the source entity (e.g., person_amara-okeke)"
    },
    "target_id": {
      "type": "string",
      "description": "Canonical ID of the target entity (e.g., startup_openai)"
    },
    "relation_type": {
      "type": "string",
      "enum": ["FOUNDED_BY", "BUILDS_PRODUCT", "PUBLISHED_PAPER", "CITES", "WORKS_AT"],
      "description": "Relationship type (PDF §8.2)"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Confidence from extraction + resolution stage"
    },
    "source_url": {
      "type": "string",
      "format": "uri",
      "description": "Original page the relationship was extracted from"
    },
    "source_content_hash": {
      "type": "string",
      "description": "Content hash of the source page for provenance"
    },
    "evidence_text": {
      "type": "string",
      "description": "Text span from which the relationship was extracted"
    },
    "collected_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 extraction timestamp"
    }
  }
}
